import 'package:flutter/material.dart';

class CertificatesView extends StatelessWidget {
  const CertificatesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
